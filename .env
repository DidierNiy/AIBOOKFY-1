# Frontend Environment Configuration
# Local dev: set to http://localhost:5003
# Production (Docker): leave empty — nginx proxies /api/ to the backend container
VITE_BACKEND_URL=
REACT_APP_BACKEND_URL=
